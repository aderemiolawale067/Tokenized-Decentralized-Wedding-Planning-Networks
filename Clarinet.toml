[project]
name = 'Tokenized-Decentralized-Wedding-Planning-Networks'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.budget-allocation]
path = 'contracts/budget-allocation.clar'
clarity_version = 2
epoch = 2.5

[contracts.conflict-resolution]
path = 'contracts/conflict-resolution.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
